{
  "name": "2dobetter-groq-agent",
  "version": "1.0.0",
  "description": "Groq (Llama 4) agent for the 2Do Better task board",
  "type": "module",
  "scripts": {
    "start": "NODE_TLS_REJECT_UNAUTHORIZED=0 node --no-warnings agent.mjs",
    "chat": "NODE_TLS_REJECT_UNAUTHORIZED=0 node --no-warnings agent.mjs --chat"
  },
  "dependencies": {
    "groq-sdk": "^0.9.0"
  },
  "engines": {
    "node": ">=18"
  }
}
